{
  "//": "Isomorphic pure TypeScript: the cue model, the .cueshow codec, and the sequencing that the desktop app keeps on its message thread. No DOM, no Node, no wasm — that is what lets the sequencer be tested without an AudioContext. Unbuilt, like the simpleVIS packages: the app's bundler compiles it from source.",
  "name": "@webcue/core",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  }
}
